# Optional overrides. The contact form uses Web3Forms (https://web3forms.com);
# the access key is public and lives directly in src/components/ContactForm.jsx.

# Email address shown on the site as the direct contact address.
VITE_CONTACT_EMAIL=info@karvicare.com.au
